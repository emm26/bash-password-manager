# bash_password_manager
Using a UNIX terminal, this script allows you to manage a passwords encripted text file. Given an entered name of an account, searches for it in the passwords file, shows the username for the desired account and pastes its password to the clipboard. 
